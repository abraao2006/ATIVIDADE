#include <iostream>
#include <string>

using namespace std;

int main() {
    string nome;

    cout << "Digite o nome: ";
    getline(cin, nome);

    if (nome[0] == 'a' || nome[0] == 'a') {
        cout << nome << endl;
    }

    return 0;
}